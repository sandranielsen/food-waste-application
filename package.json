{
  "name": "food-waste-spa",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "run": "parcel --open  --no-optimize",
    "build": "parcel build --no-optimize"
  },
  "devDependencies": {
    "parcel": "^2.0.1"
  }
}
